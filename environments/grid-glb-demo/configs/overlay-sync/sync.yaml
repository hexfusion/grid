source:
  context: kind-grid-glb-site-us-east
  namespace: grid-system
  configMap: grid-overlay-glb-demo-consumer-gateway
  key: grid-config.json

output:
  path: /output/grid-config.json
  format: raw

watch:
  interval: "5s"
